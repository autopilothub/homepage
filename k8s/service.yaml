apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: autopilothub
  labels:
    app: homepage
spec:
  type: ClusterIP
  selector:
    app: homepage
  ports:
    - port: 80
      targetPort: 3000
